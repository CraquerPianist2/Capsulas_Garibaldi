name: iOS Build (Simulator)

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build iOS Simulator Framework
        # Usamos el nombre exacto de la tarea generado por KMP
        run: ./gradlew :app:linkDebugFrameworkIosSimulatorArm64

      - name: Upload iOS Framework Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-framework-debug
          path: |
            app/build/bin/iosSimulatorArm64/debugFramework/
            app/build/fat-framework/debug/
          if-no-files-found: warn
